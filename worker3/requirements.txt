# Worker 3 — The Doctor
kafka-python==2.0.2
python-dotenv==1.0.1
httpx==0.27.0

# WebSocket server (dashboard broadcast)
websockets==12.0

# LangGraph — pipeline/flowchart manager
langgraph==0.2.28
langchain-core==0.3.15

# ChromaDB — vector store for past fixes
chromadb==0.4.24

# Reuse Worker 2 checks
pandera==0.20.4
pandas==2.2.2
fuzzywuzzy==0.18.0
python-Levenshtein==0.25.1
metaphone==0.6
numpy<2.0

chromadb==0.5.3
numpy<2.0